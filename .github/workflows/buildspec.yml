version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
      - $CODEBUILD_SRC_DIR
artifacts:
  files:
    - './android/app/build/outputs/**/*.apk'
  discard-paths: yes